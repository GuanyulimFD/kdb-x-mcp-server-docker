__pycache__*
*.py[cod]
*$py.class
.venv
.env
.coverage
coverage.xml
report.xml
out
copilot_exports
.vscode/settings.json
.specstory
.pytest_cache/
# KDB-X service logs (timestamped, generated at runtime)
logs/*.log
logs/kdbx.pid
developer-1.5.4*

# Vendored ax-libraries (binary .so/.dylib files — install via scripts/install_ax_libraries.sh)
vendor/ax-libraries/
vendor/ax-libraries.zip
.DS_Store
.github/kdb-knowhow.md

# Local JIRA / task memory — agent handover files, not for version control
.memory/
research/
memos/